# This file contains general commands and tips that can be useful in R projects.

# selected line(s) can be run using Ctrl+Enter

# Check the working directory
getwd()

# Change to the American settings:
Sys.setlocale("LC_ALL", "C")
